# Cloud Build 업로드 범위는 Docker 빌드 컨텍스트와 똑같이 유지한다.
# 두 벌로 관리하면 한쪽만 고쳐 어긋나므로 .dockerignore 를 그대로 가져온다.
# (.dockerignore 는 Dockerfile 자신을 제외하지 않는다 — 제외하면 여기서 업로드가 빠져
#  Cloud Build 가 Dockerfile 을 찾지 못한다.)
#!include:.dockerignore
