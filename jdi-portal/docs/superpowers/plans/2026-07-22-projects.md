# 프로젝트(프로젝트별 타임라인) 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 타임라인·할일 공통 "프로젝트" 분류를 도입한다 — projects 테이블 + 작성 시 선택 + 사이드바 하위 메뉴(전체/프로젝트별/미분류) + 기존 글 접두어 자동 분류.

**Architecture:** 정식 `projects` 테이블(FK, `ON DELETE SET NULL`)을 만들고, 타임라인은 PostgREST 조인으로 `entry.project`를, 할일은 `TASK_BASE_SELECT`/pg 빠른 경로 양쪽에 project를 싣는다. 프로젝트 목록은 클라이언트 훅 `useProjects()`(모듈 캐시 + `jdi:projects-changed` 이벤트)로 사이드바/모달/필터가 공유한다. URL 상태는 `?project=<uuid|none>` 하나로 사이드바·타임라인 필터를 일원화한다.

**Tech Stack:** Next.js 16 App Router, Supabase(RLS), pg Pool 빠른 경로, node:test 정적 테스트.

**Spec:** `docs/superpowers/specs/2026-07-22-projects-design.md`

## Global Constraints

- 마이그레이션 번호는 **101** (`supabase/migrations/101_projects.sql`). 기존 파일 수정 금지, 추가만.
- 모든 명령은 `jdi-portal/` 안에서 실행 (`cd jdi-portal`).
- 프로젝트 권한: 보기/생성/수정 = 승인 사용자 전체, **삭제 = admin만** (RLS + 서버 액션 이중 검증).
- 프로젝트 선택은 **선택사항** — 미선택은 `project_id IS NULL`(미분류). URL 값은 `""`(전체) / `"none"`(미분류) / UUID.
- 이름 1~50자(트림), 중복 금지(`lower(btrim(name))` 유니크), 색상 `#rrggbb`.
- 초기 프로젝트: `코스피랩`, `TMA`, `JDI 포탈`. 접두어 `^이름\s*[-–—:]\s*` 자동 분류 + 접두어 제거. `오너먼트|트리` 키워드 타임라인 글 → TMA (제목 유지).
- **성능 불변조건**: 미들웨어 인증 캐시·keepalive·초기 JS 예산·088 사전 필터를 건드리지 않는다. 할일 project 표시는 **fast-queries(pg)와 REST 폴백 양쪽**에 함께 넣는다.
- 사이드바 하위 링크는 반드시 `prefetch={false}`.
- Supabase `error`는 항상 처리. `git push`는 하지 않는다(로컬 커밋만). **운영 DB 적용(db push)은 Task 11에서 사용자 확인 후에만.**
- 정적 테스트는 `scripts/projects-feature.test.mjs` 하나에 누적하며, 각 태스크에서 해당 블록을 먼저 추가(실패 확인) 후 구현한다. 실행: `node --test scripts/projects-feature.test.mjs`
- 스펙과 다른 점(승인된 변경): 할일의 "그룹 보기 프로젝트별"은 현재 UI에 그룹 보기가 없어(구버전 잔재 `groupTasks`) **할일 기록의 프로젝트 필터**로 대체한다. Task 9에서 스펙 문서도 갱신.

---

### Task 1: DB 마이그레이션 `101_projects.sql`

**Files:**
- Create: `supabase/migrations/101_projects.sql`
- Create: `scripts/projects-feature.test.mjs`

**Interfaces:**
- Produces: `public.projects(id, name, color, is_archived, created_by, created_at, updated_at)`, `work_timeline_entries.project_id`, `tasks.project_id` (둘 다 `uuid NULL REFERENCES projects ON DELETE SET NULL`)

- [ ] **Step 1: 정적 테스트 작성 (실패 확인용)**

`scripts/projects-feature.test.mjs` 생성:

```js
import test from "node:test";
import assert from "node:assert/strict";
import { readFileSync, existsSync } from "node:fs";
import { join } from "node:path";

const read = (p) => readFileSync(join(process.cwd(), p), "utf8");

test("101 마이그레이션: projects 테이블 + RLS + FK 컬럼", () => {
  const path = "supabase/migrations/101_projects.sql";
  assert.ok(existsSync(join(process.cwd(), path)), "101_projects.sql 이 없습니다");
  const sql = read(path);
  assert.match(sql, /CREATE TABLE public\.projects/);
  assert.match(sql, /ALTER TABLE public\.projects ENABLE ROW LEVEL SECURITY/);
  // 삭제는 admin만
  assert.match(sql, /FOR DELETE[\s\S]*?role = 'admin'/);
  // 이름 중복 방지(대소문자·공백 무시)
  assert.match(sql, /lower\(btrim\(name\)\)/);
  // 프로젝트 삭제 시 글/할일은 미분류로 (created_by 포함 3회 이상)
  assert.ok((sql.match(/ON DELETE SET NULL/g) ?? []).length >= 3);
  assert.match(sql, /ALTER TABLE public\.work_timeline_entries\s+ADD COLUMN project_id/);
  assert.match(sql, /ALTER TABLE public\.tasks\s+ADD COLUMN project_id/);
  // 부분 인덱스
  assert.match(sql, /idx_work_timeline_entries_project[\s\S]*?WHERE project_id IS NOT NULL/);
  assert.match(sql, /idx_tasks_project[\s\S]*?WHERE project_id IS NOT NULL/);
  // 초기 데이터 + 접두어 자동 분류
  assert.match(sql, /코스피랩/);
  assert.match(sql, /JDI 포탈/);
  assert.match(sql, /regexp_replace/);
  assert.match(sql, /오너먼트/);
  // KST 규칙 위반 금지: 날짜 컬럼 없음 → CURRENT_DATE 미사용
  assert.doesNotMatch(sql, /CURRENT_DATE/);
});
```

- [ ] **Step 2: 실패 확인**

Run: `cd jdi-portal; node --test scripts/projects-feature.test.mjs`
Expected: FAIL — `101_projects.sql 이 없습니다`

- [ ] **Step 3: 마이그레이션 작성**

`supabase/migrations/101_projects.sql`:

```sql
-- ============================================================
-- 101: 프로젝트 (타임라인·할일 공통 분류)
--  - projects 테이블 + RLS (보기/생성/수정 = 승인 사용자, 삭제 = admin)
--  - work_timeline_entries.project_id, tasks.project_id (ON DELETE SET NULL)
--  - 초기 프로젝트 3개 + 기존 글 제목 접두어 자동 분류(접두어 제거)
--  주의: 접두어 제거는 되돌릴 수 없다. 적용 전 백업 시점 확인.
-- ============================================================

CREATE TABLE public.projects (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  name text NOT NULL,
  color text NOT NULL DEFAULT '#6366f1',
  is_archived boolean NOT NULL DEFAULT false,
  created_by uuid REFERENCES public.profiles(id) ON DELETE SET NULL,
  created_at timestamptz NOT NULL DEFAULT now(),
  updated_at timestamptz NOT NULL DEFAULT now(),
  CONSTRAINT projects_name_check CHECK (char_length(btrim(name)) BETWEEN 1 AND 50),
  CONSTRAINT projects_color_check CHECK (color ~ '^#[0-9a-fA-F]{6}$')
);

CREATE UNIQUE INDEX idx_projects_name_unique ON public.projects (lower(btrim(name)));

CREATE TRIGGER projects_set_updated_at
  BEFORE UPDATE ON public.projects
  FOR EACH ROW EXECUTE FUNCTION public.set_updated_at();

ALTER TABLE public.projects ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Approved users can view projects"
  ON public.projects FOR SELECT TO authenticated
  USING (public.is_approved_user());

CREATE POLICY "Approved users can create projects"
  ON public.projects FOR INSERT TO authenticated
  WITH CHECK (public.is_approved_user() AND created_by = auth.uid());

CREATE POLICY "Approved users can update projects"
  ON public.projects FOR UPDATE TO authenticated
  USING (public.is_approved_user())
  WITH CHECK (public.is_approved_user());

CREATE POLICY "Admins can delete projects"
  ON public.projects FOR DELETE TO authenticated
  USING (
    public.is_approved_user()
    AND EXISTS (
      SELECT 1 FROM public.profiles
      WHERE id = auth.uid() AND role = 'admin'
    )
  );

-- ============================================================
-- 연결 컬럼 (프로젝트 삭제 시 글/할일은 미분류로 복귀)
-- ============================================================

ALTER TABLE public.work_timeline_entries
  ADD COLUMN project_id uuid REFERENCES public.projects(id) ON DELETE SET NULL;

ALTER TABLE public.tasks
  ADD COLUMN project_id uuid REFERENCES public.projects(id) ON DELETE SET NULL;

CREATE INDEX idx_work_timeline_entries_project
  ON public.work_timeline_entries (project_id) WHERE project_id IS NOT NULL;

CREATE INDEX idx_tasks_project
  ON public.tasks (project_id) WHERE project_id IS NOT NULL;

-- ============================================================
-- 초기 프로젝트 + 기존 글 자동 분류
-- ============================================================

INSERT INTO public.projects (name, color)
VALUES
  ('코스피랩', '#6366f1'),
  ('TMA', '#16a34a'),
  ('JDI 포탈', '#f59e0b')
ON CONFLICT (lower(btrim(name))) DO NOTHING;

-- 제목이 "코스피랩 - ", "TMA - ", "JDI 포탈 - " 형태로 시작하는 글을
-- 해당 프로젝트로 묶고 접두어를 제거한다. 제거 후 제목이 비면 원제목 유지.
DO $$
DECLARE
  rec record;
BEGIN
  FOR rec IN
    SELECT p.id AS project_id, pat.pattern
    FROM (VALUES
      ('코스피랩', '^\s*코스피랩\s*[-–—:]\s*'),
      ('TMA',     '^\s*TMA\s*[-–—:]\s*'),
      ('JDI 포탈', '^\s*JDI\s*포탈\s*[-–—:]\s*')
    ) AS pat(name, pattern)
    JOIN public.projects p ON lower(btrim(p.name)) = lower(pat.name)
  LOOP
    UPDATE public.work_timeline_entries e
    SET project_id = rec.project_id,
        title = CASE
          WHEN char_length(btrim(regexp_replace(e.title, rec.pattern, '', 'i'))) BETWEEN 1 AND 120
            THEN btrim(regexp_replace(e.title, rec.pattern, '', 'i'))
          ELSE e.title
        END
    WHERE e.project_id IS NULL
      AND e.title ~* rec.pattern;

    UPDATE public.tasks t
    SET project_id = rec.project_id,
        title = CASE
          WHEN char_length(btrim(regexp_replace(t.title, rec.pattern, '', 'i'))) >= 1
            THEN btrim(regexp_replace(t.title, rec.pattern, '', 'i'))
          ELSE t.title
        END
    WHERE t.project_id IS NULL
      AND t.title ~* rec.pattern;
  END LOOP;
END $$;

-- 트리 오너먼트 관련 타임라인 글 → TMA (크리스마스 트리 브랜드, 제목은 유지)
UPDATE public.work_timeline_entries e
SET project_id = p.id
FROM public.projects p
WHERE lower(btrim(p.name)) = 'tma'
  AND e.project_id IS NULL
  AND (e.title ~* '오너먼트' OR e.title ~* '트리');
```

참고: `work_timeline_entries`의 identity 트리거(083)는 user_id/task_id만 막으므로 위 UPDATE는 통과한다. `set_updated_at` 트리거가 updated_at을 갱신하지만 목록 정렬은 completed_at 기준이라 영향 없다.

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd jdi-portal; node --test scripts/projects-feature.test.mjs`
Expected: PASS (1 test)

- [ ] **Step 5: 커밋**

```bash
git add jdi-portal/supabase/migrations/101_projects.sql jdi-portal/scripts/projects-feature.test.mjs
git commit -m "DB: projects 테이블 + 타임라인·할일 project_id + 접두어 자동 분류 (101)"
```

(⚠️ 운영 DB 적용은 Task 11에서. 여기서는 파일만 만든다.)

---

### Task 2: `src/lib/projects/` 도메인 모듈

**Files:**
- Create: `src/lib/projects/types.ts`
- Create: `src/lib/projects/constants.ts`
- Create: `src/lib/projects/queries.ts`
- Create: `src/lib/projects/actions.ts`
- Create: `src/lib/projects/useProjects.ts`
- Modify: `scripts/projects-feature.test.mjs` (블록 추가)

**Interfaces:**
- Produces:
  - `Project { id: string; name: string; color: string; is_archived: boolean; created_by: string | null; created_at: string; updated_at: string }`
  - `ProjectRef { id: string; name: string; color: string }`
  - `getProjects(supabase, { includeArchived? }): Promise<Project[]>` (이름순)
  - `createProject(name: string, color: string): Promise<Project>`
  - `updateProject(id: string, input: { name?: string; color?: string; isArchived?: boolean }): Promise<Project>`
  - `deleteProject(id: string): Promise<void>` — admin 아니면 throw
  - `useProjects(): { projects: Project[]; activeProjects: Project[]; loaded: boolean }` (클라이언트 훅, 모듈 캐시)
  - `notifyProjectsChanged(): void` — 캐시 무효화 + `jdi:projects-changed` 이벤트
  - `PROJECT_COLORS: readonly string[]` (8색), `PROJECT_NAME_MAX_LENGTH = 50`

- [ ] **Step 1: 테스트 블록 추가**

`scripts/projects-feature.test.mjs` 끝에 추가:

```js
test("lib/projects: 모듈 구성과 서버 검증", () => {
  const actions = read("src/lib/projects/actions.ts");
  assert.match(actions, /^"use server";/);
  // 삭제는 서버에서도 admin 재검증 (RLS만 믿지 않음)
  assert.match(actions, /role[\s\S]*?admin/);
  // Supabase error 무시 금지
  assert.doesNotMatch(actions, /\.insert\([\s\S]*?\)\s*;\s*return/);
  const queries = read("src/lib/projects/queries.ts");
  assert.match(queries, /order\("name"/);
  const hook = read("src/lib/projects/useProjects.ts");
  assert.match(hook, /jdi:projects-changed/);
});
```

- [ ] **Step 2: 실패 확인** — Run: `node --test scripts/projects-feature.test.mjs` → FAIL (파일 없음)

- [ ] **Step 3: 구현**

`src/lib/projects/types.ts`:

```ts
export interface Project {
  id: string;
  name: string;
  color: string;
  is_archived: boolean;
  created_by: string | null;
  created_at: string;
  updated_at: string;
}

/** 조인 결과 등 최소 표시용 */
export interface ProjectRef {
  id: string;
  name: string;
  color: string;
}
```

`src/lib/projects/constants.ts`:

```ts
export const PROJECT_NAME_MAX_LENGTH = 50;

/** 프로젝트 색상 팔레트 (고정 선택지) */
export const PROJECT_COLORS = [
  "#6366f1", // 인디고
  "#0ea5e9", // 하늘
  "#16a34a", // 초록
  "#f59e0b", // 주황
  "#ef4444", // 빨강
  "#8b5cf6", // 보라
  "#ec4899", // 분홍
  "#64748b", // 회색
] as const;
```

`src/lib/projects/queries.ts`:

```ts
import type { SupabaseClient } from "@supabase/supabase-js";
import type { Project } from "./types";

export async function getProjects(
  supabase: SupabaseClient,
  options: { includeArchived?: boolean } = {},
): Promise<Project[]> {
  let request = supabase
    .from("projects")
    .select("id, name, color, is_archived, created_by, created_at, updated_at")
    .order("name", { ascending: true });
  if (!options.includeArchived) request = request.eq("is_archived", false);
  const { data, error } = await request;
  if (error) throw error;
  return (data ?? []) as Project[];
}
```

`src/lib/projects/actions.ts`:

```ts
"use server";

import { revalidatePath } from "next/cache";
import { createClient } from "@/lib/supabase/server";
import { PROJECT_COLORS, PROJECT_NAME_MAX_LENGTH } from "./constants";
import type { Project } from "./types";

const UUID_PATTERN = /^[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
const COLOR_PATTERN = /^#[0-9a-fA-F]{6}$/;
const PROJECT_SELECT = "id, name, color, is_archived, created_by, created_at, updated_at";

type ServerClient = Awaited<ReturnType<typeof createClient>>;

async function getAuthenticatedContext(): Promise<{ supabase: ServerClient; userId: string }> {
  const supabase = await createClient();
  const { data, error } = await supabase.auth.getUser();
  if (error || !data.user) throw new Error("로그인이 필요합니다.");
  return { supabase, userId: data.user.id };
}

function revalidateProjectViews(): void {
  revalidatePath("/dashboard");
  revalidatePath("/dashboard/work-timeline");
  revalidatePath("/dashboard/tasks");
}

function validateName(name: string): string {
  const trimmed = name.trim();
  if (!trimmed) throw new Error("프로젝트 이름을 입력해 주세요.");
  if (trimmed.length > PROJECT_NAME_MAX_LENGTH) {
    throw new Error(`프로젝트 이름은 ${PROJECT_NAME_MAX_LENGTH}자 이하로 입력해 주세요.`);
  }
  return trimmed;
}

function validateColor(color: string): string {
  if (!COLOR_PATTERN.test(color)) throw new Error("프로젝트 색상이 올바르지 않습니다.");
  return color.toLowerCase();
}

function assertProjectId(id: string): void {
  if (!UUID_PATTERN.test(id)) throw new Error("프로젝트 값이 올바르지 않습니다.");
}

function isUniqueViolation(error: unknown): boolean {
  return Boolean(
    error && typeof error === "object" && "code" in error
      && (error as { code?: string }).code === "23505",
  );
}

export async function createProject(name: string, color?: string): Promise<Project> {
  const { supabase, userId } = await getAuthenticatedContext();
  const { data, error } = await supabase
    .from("projects")
    .insert({
      name: validateName(name),
      color: validateColor(color || PROJECT_COLORS[0]),
      created_by: userId,
    })
    .select(PROJECT_SELECT)
    .single();
  if (error) {
    if (isUniqueViolation(error)) throw new Error("같은 이름의 프로젝트가 이미 있습니다.");
    throw error;
  }
  revalidateProjectViews();
  return data as Project;
}

export async function updateProject(
  id: string,
  input: { name?: string; color?: string; isArchived?: boolean },
): Promise<Project> {
  assertProjectId(id);
  const { supabase } = await getAuthenticatedContext();
  const payload: Record<string, unknown> = {};
  if (input.name !== undefined) payload.name = validateName(input.name);
  if (input.color !== undefined) payload.color = validateColor(input.color);
  if (input.isArchived !== undefined) payload.is_archived = input.isArchived;
  if (Object.keys(payload).length === 0) throw new Error("변경할 내용이 없습니다.");

  const { data, error } = await supabase
    .from("projects")
    .update(payload)
    .eq("id", id)
    .select(PROJECT_SELECT)
    .single();
  if (error) {
    if (isUniqueViolation(error)) throw new Error("같은 이름의 프로젝트가 이미 있습니다.");
    throw error;
  }
  revalidateProjectViews();
  return data as Project;
}

export async function deleteProject(id: string): Promise<void> {
  assertProjectId(id);
  const { supabase, userId } = await getAuthenticatedContext();
  // RLS가 최종 방어선이지만 서버에서도 admin을 재검증한다.
  const { data: profile, error: profileError } = await supabase
    .from("profiles")
    .select("role")
    .eq("id", userId)
    .single();
  if (profileError) throw profileError;
  if (profile.role !== "admin") throw new Error("프로젝트 삭제는 관리자만 할 수 있습니다.");

  const { error } = await supabase.from("projects").delete().eq("id", id);
  if (error) throw error;
  revalidateProjectViews();
}
```

`src/lib/projects/useProjects.ts`:

```ts
"use client";

import { useEffect, useState } from "react";
import { createClient } from "@/lib/supabase/client";
import { getProjects } from "./queries";
import type { Project } from "./types";

const PROJECTS_CHANGED_EVENT = "jdi:projects-changed";

let cachedProjects: Project[] | null = null;
let inflight: Promise<Project[]> | null = null;

async function fetchProjects(): Promise<Project[]> {
  if (!inflight) {
    inflight = getProjects(createClient(), { includeArchived: true })
      .then((projects) => {
        cachedProjects = projects;
        return projects;
      })
      .finally(() => {
        inflight = null;
      });
  }
  return inflight;
}

/** 생성/수정/삭제 후 호출 — 캐시를 비우고 모든 구독 컴포넌트를 갱신한다. */
export function notifyProjectsChanged(): void {
  cachedProjects = null;
  if (typeof window !== "undefined") {
    window.dispatchEvent(new Event(PROJECTS_CHANGED_EVENT));
  }
}

/**
 * 프로젝트 목록 클라이언트 훅.
 * - 모듈 레벨 캐시로 대시보드 내 중복 조회를 막는다 (표시용, 권한은 RLS가 담당)
 * - `projects` = 보관 포함 전체, `activeProjects` = 보관 제외
 */
export function useProjects(): {
  projects: Project[];
  activeProjects: Project[];
  loaded: boolean;
} {
  const [projects, setProjects] = useState<Project[]>(cachedProjects ?? []);
  const [loaded, setLoaded] = useState(cachedProjects !== null);

  useEffect(() => {
    let active = true;
    const load = () => {
      void fetchProjects()
        .then((next) => {
          if (!active) return;
          setProjects(next);
          setLoaded(true);
        })
        .catch((error) => {
          console.warn("[projects] 목록 조회 실패:", error);
          if (active) setLoaded(true);
        });
    };
    if (cachedProjects === null) load();
    window.addEventListener(PROJECTS_CHANGED_EVENT, load);
    return () => {
      active = false;
      window.removeEventListener(PROJECTS_CHANGED_EVENT, load);
    };
  }, []);

  return {
    projects,
    activeProjects: projects.filter((project) => !project.is_archived),
    loaded,
  };
}
```

- [ ] **Step 4: 테스트 통과 + 타입 확인**

Run: `node --test scripts/projects-feature.test.mjs` → PASS
Run: `npm run lint` → 오류 없음

- [ ] **Step 5: 커밋**

```bash
git add jdi-portal/src/lib/projects jdi-portal/scripts/projects-feature.test.mjs
git commit -m "기능: projects 도메인 모듈(queries/actions/useProjects 훅)"
```

---

### Task 3: 타임라인 lib 연동 (types/queries/actions/draft/캐시)

**Files:**
- Modify: `src/lib/work-timeline/types.ts`
- Modify: `src/lib/work-timeline/queries.ts:11-14` (ENTRY_SELECT), `:100-143` (필터)
- Modify: `src/lib/work-timeline/actions.ts` (create/update/select 문자열)
- Modify: `src/lib/work-timeline/draftStore.ts` (projectId)
- Modify: `src/lib/work-timeline/timelineCache.ts:17` (스키마 버전)
- Modify: `scripts/projects-feature.test.mjs`

**Interfaces:**
- Consumes: `ProjectRef` (`@/lib/projects/types`)
- Produces:
  - `WorkTimelineEntry.project_id: string | null`
  - `WorkTimelineEntryWithProfile.project: ProjectRef | null`
  - `WorkTimelineFilters.projectId?: string | null` — `"none"` = 미분류, UUID = 해당 프로젝트
  - `CreateWorkTimelineEntryInput.projectId?: string | null`, `UpdateWorkTimelineEntryInput.projectId?: string | null` (undefined = 변경 없음)
  - `WorkTimelineDraftPayload.projectId?: string | null`

- [ ] **Step 1: 테스트 블록 추가**

```js
test("work-timeline: project 조인·필터·캐시 버전", () => {
  const queries = read("src/lib/work-timeline/queries.ts");
  assert.match(queries, /project:projects\(id, name, color\)/);
  assert.match(queries, /is\("project_id", null\)/);
  const actions = read("src/lib/work-timeline/actions.ts");
  assert.match(actions, /project_id/);
  const cache = read("src/lib/work-timeline/timelineCache.ts");
  assert.match(cache, /CACHE_SCHEMA_VERSION = 3/);
});
```

Run: `node --test scripts/projects-feature.test.mjs` → 새 블록 FAIL 확인

- [ ] **Step 2: types.ts 수정**

- `WorkTimelineEntry`에 `task_id` 아래 한 줄 추가: `project_id: string | null;`
- 파일 상단 import 추가: `import type { ProjectRef } from "@/lib/projects/types";`
- `WorkTimelineEntryWithProfile`에 추가: `project: ProjectRef | null;`
- `WorkTimelineFilters`에 추가: `projectId?: string | null; // "none" = 미분류`
- `CreateWorkTimelineEntryInput`에 추가: `projectId?: string | null;`
- `UpdateWorkTimelineEntryInput`에 추가: `projectId?: string | null;`

- [ ] **Step 3: queries.ts 수정**

ENTRY_SELECT 교체:

```ts
const ENTRY_SELECT = `
  id, user_id, task_id, project_id, title, description, completed_at, created_at, updated_at,
  author_profile:profiles!work_timeline_entries_user_id_fkey(id, full_name, avatar_url),
  project:projects(id, name, color)
`;
```

`getWorkTimelineEntries`의 `if (filters.employeeId) ...` 다음 줄에 추가:

```ts
  if (filters.projectId === "none") request = request.is("project_id", null);
  else if (filters.projectId) {
    assertUuid(filters.projectId, "프로젝트");
    request = request.eq("project_id", filters.projectId);
  }
```

- [ ] **Step 4: actions.ts 수정**

엔트리 컬럼 select 문자열 3곳(`findDuplicateTaskEntry`, `insertEntry`의 `.select`, `updateWorkTimelineEntry`의 `.select`)을 모두 다음으로 교체:

```
"id, user_id, task_id, project_id, title, description, completed_at, created_at, updated_at"
```

`insertEntry`에서 `const values = validateWorkTimelineInput(input);` 바로 뒤에 추가:

```ts
  if (input.projectId) assertUuid(input.projectId, "프로젝트");
```

insert 페이로드에 `task_id` 다음 추가: `project_id: input.projectId || null,`

`updateWorkTimelineEntry`의 `.update({...})` 호출을 다음 구조로 교체 (projectId가 undefined면 유지):

```ts
  const updatePayload: Record<string, unknown> = {
    title: values.title,
    description: values.description,
    completed_at: values.completedAt,
    updated_at: new Date().toISOString(),
  };
  if (input.projectId !== undefined) {
    if (input.projectId) assertUuid(input.projectId, "프로젝트");
    updatePayload.project_id = input.projectId;
  }
  const { data, error } = await supabase
    .from("work_timeline_entries")
    .update(updatePayload)
    .eq("id", entryId)
    .select("id, user_id, task_id, project_id, title, description, completed_at, created_at, updated_at")
    .single();
```

- [ ] **Step 5: draftStore.ts / timelineCache.ts**

- `WorkTimelineDraftPayload`에 추가: `projectId?: string | null;` (기존 저장 레코드는 필드가 없음 → 읽는 쪽에서 `?? null`)
- `timelineCache.ts`: `const CACHE_SCHEMA_VERSION = 2;` → `3` (project 필드가 없는 구캐시 무효화)

- [ ] **Step 6: 검증 + 커밋**

Run: `node --test scripts/projects-feature.test.mjs` → PASS, `npm run lint` → 오류 없음

```bash
git add jdi-portal/src/lib/work-timeline jdi-portal/scripts/projects-feature.test.mjs
git commit -m "기능: 타임라인 lib에 project 연동(조인·필터·작성·초안·캐시버전)"
```

---

### Task 4: 사이드바 하위 메뉴

**Files:**
- Modify: `src/components/dashboard/Sidebar.tsx`
- Modify: `scripts/projects-feature.test.mjs`

**Interfaces:**
- Consumes: `useProjects()` (Task 2)
- URL 규약: 전체 = `/dashboard/work-timeline`, 프로젝트 = `?project=<uuid>`, 미분류 = `?project=none`

- [ ] **Step 1: 테스트 블록 추가**

```js
test("사이드바: 타임라인 하위 메뉴", () => {
  const sidebar = read("src/components/dashboard/Sidebar.tsx");
  assert.match(sidebar, /useProjects/);
  assert.match(sidebar, /project=none/);
  assert.match(sidebar, /미분류/);
  // 하위 링크 포함 모든 Link 는 prefetch 금지 유지
  assert.doesNotMatch(sidebar, /prefetch=\{true\}/);
});
```

Run → FAIL 확인

- [ ] **Step 2: 구현**

Sidebar.tsx 수정:

1. import 추가:

```ts
import { useSearchParams } from "next/navigation";
import { useProjects } from "@/lib/projects/useProjects";
```

(`usePathname` import 줄에 `useSearchParams`를 합쳐도 됨: `import { usePathname, useSearchParams } from "next/navigation";`)

2. 컴포넌트 본문 상단(`const pathname = usePathname();` 아래)에 추가:

```ts
  const searchParams = useSearchParams();
  const { activeProjects } = useProjects();
  const onTimeline = pathname.startsWith("/dashboard/work-timeline");
  const currentProject = searchParams.get("project") ?? "";
  const timelineSubItems = [
    { value: "", label: "전체", color: null as string | null },
    ...activeProjects.map((project) => ({
      value: project.id,
      label: project.name,
      color: project.color as string | null,
    })),
    { value: "none", label: "미분류", color: null as string | null },
  ];
```

3. `navItems.map` 안에서 기존 `<Link ...>...</Link>` 반환부를 fragment 로 감싸고, Link 뒤에 하위 메뉴를 추가한다. `return (` 바로 뒤 구조:

```tsx
          return (
            <div key={item.href}>
              <Link
                href={item.href}
                prefetch={false}
                ...기존 그대로 (key만 위 div로 이동)...
              </Link>
              {!collapsed && item.href === "/dashboard/work-timeline" && onTimeline && (
                <div className="mt-1 space-y-0.5 pl-9 pr-1">
                  {timelineSubItems.map((sub) => {
                    const subActive = currentProject === sub.value;
                    return (
                      <Link
                        key={sub.value || "all"}
                        href={sub.value
                          ? `/dashboard/work-timeline?project=${sub.value}`
                          : "/dashboard/work-timeline"}
                        prefetch={false}
                        onClick={onMobileClose}
                        className={`flex items-center gap-2 rounded-lg px-2.5 py-1.5 text-xs font-semibold transition-colors ${
                          subActive
                            ? "bg-brand-50 text-brand-600"
                            : "text-slate-500 hover:bg-slate-100 hover:text-slate-800"
                        }`}
                      >
                        {sub.color ? (
                          <span
                            className="h-2 w-2 shrink-0 rounded-full"
                            style={{ backgroundColor: sub.color }}
                            aria-hidden="true"
                          />
                        ) : (
                          <span className="h-2 w-2 shrink-0 rounded-full bg-slate-300" aria-hidden="true" />
                        )}
                        <span className="truncate">{sub.label}</span>
                      </Link>
                    );
                  })}
                </div>
              )}
            </div>
          );
```

주의: 기존 Link의 `key={item.href}`는 감싸는 `<div key={item.href}>`로 옮긴다. 접힘(collapsed) 상태와 모바일이 아닌 화면에서 자동으로 동작한다(모바일 오버레이도 같은 sidebarContent 를 씀).

참고: 대시보드 페이지는 인증 쿠키로 항상 동적 렌더링이므로 `useSearchParams` 사용에 Suspense 가 필요 없다. 만약 `npm run build`에서 Suspense 경고/오류가 나면 하위 메뉴 블록을 별도 클라이언트 컴포넌트로 분리해 `<Suspense fallback={null}>`로 감싼다.

- [ ] **Step 3: 검증 + 커밋**

Run: `node --test scripts/projects-feature.test.mjs` → PASS, `npm run lint`

```bash
git add jdi-portal/src/components/dashboard/Sidebar.tsx jdi-portal/scripts/projects-feature.test.mjs
git commit -m "기능: 사이드바 업무 타임라인 하위 메뉴(전체/프로젝트/미분류)"
```

---

### Task 5: 타임라인 페이지 + Section (필터·배지·URL 동기화)

**Files:**
- Modify: `src/app/dashboard/work-timeline/page.tsx`
- Modify: `src/components/dashboard/work-timeline/WorkTimelineSection.tsx`
- Modify: `scripts/projects-feature.test.mjs`

**Interfaces:**
- Consumes: `WorkTimelineFilters.projectId`(Task 3), `useProjects`(Task 2)
- Produces: `WorkTimelineSectionProps.initialProjectId?: string`; Section 헤더의 "프로젝트 관리" 버튼은 Task 8 의 `ProjectManageModal` 을 연다(이번 태스크에서는 버튼+상태까지만 만들고 모달 import 는 Task 8 에서 추가)

- [ ] **Step 1: 테스트 블록 추가**

```js
test("타임라인 화면: project 파라미터·필터·배지", () => {
  const page = read("src/app/dashboard/work-timeline/page.tsx");
  assert.match(page, /initialProjectId/);
  const section = read("src/components/dashboard/work-timeline/WorkTimelineSection.tsx");
  assert.match(section, /updateParam\("project"/);
  assert.match(section, /entry\.project/);
  assert.match(section, /useProjects/);
});
```

Run → FAIL 확인

- [ ] **Step 2: page.tsx 수정**

- `searchParams` 타입에 `project?: string | string[];` 추가
- `const employeeId = ...` 아래 추가:

```ts
  const projectParam = firstValue(params.project);
  const initialProjectId =
    projectParam === "none" || UUID_PATTERN.test(projectParam) ? projectParam : "";
```

- `getWorkTimelineEntries` 호출 옵션에 `projectId: initialProjectId || null,` 추가
- `<WorkTimelineSection ...>`에 `initialProjectId={initialProjectId}` 추가

- [ ] **Step 3: WorkTimelineSection.tsx 수정**

1. import 추가: `import { useSearchParams } from "next/navigation";` (기존 `useRouter` 줄에 합침), `import { useProjects } from "@/lib/projects/useProjects";`
2. Props 에 `initialProjectId?: string;` 추가, 시그니처 기본값 `initialProjectId = ""`
3. 상태 추가(다른 필터 상태 옆): `const [projectId, setProjectId] = useState(initialProjectId);`
   그리고 `const { activeProjects } = useProjects();`, `const searchParams = useSearchParams();`
   ("프로젝트 관리" 버튼·`manageOpen` 상태는 미사용 lint 오류를 피하기 위해 **Task 8에서** 함께 추가한다)
4. `loadEntries` 시그니처에 5번째 파라미터 `projectFilter = projectId` 추가, `getWorkTimelineEntries` 옵션에 `projectId: projectFilter || null,` 추가, useCallback 의존성에 `projectId` 추가
5. 필터 변경 effect(`[date, employeeId, query]`)를 `[date, employeeId, query, projectId]`로 확장하고 `loadEntries(true, employeeId, date, query, projectId)` 호출로 변경. IntersectionObserver effect 의 `loadEntries(false, ...)`에도 `projectId` 전달 + 의존성 추가. `handleCreated` 도 동일.
6. URL 동기화 effect 에 `updateParam("project", projectId);` 추가 + 의존성 `projectId`
7. popstate 복원 `restoreFilters`에 추가:

```ts
      const nextProject = params.get("project") ?? "";
      setProjectId(nextProject === "none" || UUID_PATTERN.test(nextProject) ? nextProject : "");
```

8. 사이드바 Link 내비게이션 반영 effect 추가(다른 effect 들 옆):

```ts
  // 사이드바 하위 메뉴(라우터 내비게이션)로 project 파라미터가 바뀌면 필터에 반영한다.
  useEffect(() => {
    if (compact) return;
    const next = searchParams.get("project") ?? "";
    const valid = next === "none" || UUID_PATTERN.test(next) ? next : "";
    setProjectId((current) => (current === valid ? current : valid));
  }, [compact, searchParams]);
```

9. 필터 줄 그리드를 3열로 교체 — 기존 `lg:grid-cols-[minmax(0,0.8fr)_minmax(0,1.2fr)]` div 를:

```tsx
      {!compact && <div className="grid gap-2 border-b border-slate-100 px-5 py-3 lg:grid-cols-[minmax(0,0.7fr)_minmax(0,0.7fr)_minmax(0,1.2fr)]">
        <div className="min-w-0">
          <Select ... 기존 직원 Select 그대로 ... />
        </div>
        <div className="min-w-0">
          <Select
            options={[
              { value: "", label: "전체 프로젝트" },
              ...activeProjects.map((project) => ({ value: project.id, label: project.name })),
              { value: "none", label: "미분류" },
            ]}
            value={projectId}
            onChange={(v) => setProjectId(v)}
            ariaLabel="프로젝트 선택"
            className="h-9 w-full rounded-md border border-slate-200 bg-white px-3 text-xs font-semibold text-slate-600 transition-colors"
          />
        </div>
        <div className="flex min-w-0 flex-wrap gap-1.5" role="group" aria-label="완료 날짜 필터">
          ... 기존 날짜 칩/입력 그대로 ...
        </div>
      </div>}
```

10. 카드 배지 — 제목 `<h4 ...>{entry.title}</h4>` 를 다음으로 교체:

```tsx
                            <div className="mt-1.5 flex min-w-0 items-center gap-1.5">
                              {entry.project && (
                                <span className="inline-flex max-w-28 shrink-0 items-center gap-1 rounded-md bg-slate-100 px-1.5 py-0.5 text-[10px] font-bold text-slate-600">
                                  <span
                                    className="h-1.5 w-1.5 shrink-0 rounded-full"
                                    style={{ backgroundColor: entry.project.color }}
                                    aria-hidden="true"
                                  />
                                  <span className="truncate">{entry.project.name}</span>
                                </span>
                              )}
                              <h4 className="min-w-0 truncate text-sm font-bold leading-5 text-slate-800">
                                {entry.title}
                              </h4>
                            </div>
```

11. 빈 화면 조건 두 곳의 `query.length >= 2 || employeeId || date` 를 `query.length >= 2 || employeeId || date || projectId` 로 확장

- [ ] **Step 4: 검증 + 커밋**

Run: `node --test scripts/projects-feature.test.mjs` → PASS, `npm run lint`

```bash
git add jdi-portal/src/app/dashboard/work-timeline/page.tsx jdi-portal/src/components/dashboard/work-timeline/WorkTimelineSection.tsx jdi-portal/scripts/projects-feature.test.mjs
git commit -m "기능: 타임라인 프로젝트 필터·URL 동기화·카드 배지"
```

---

### Task 6: 작성 모달 프로젝트 선택 + 인라인 새 프로젝트

**Files:**
- Modify: `src/components/dashboard/work-timeline/WorkTimelineCreateModal.tsx`

**Interfaces:**
- Consumes: `useProjects`/`notifyProjectsChanged`(Task 2), `createProject`(Task 2), `CreateWorkTimelineEntryInput.projectId`(Task 3), `WorkTimelineDraftPayload.projectId`(Task 3), `PROJECT_COLORS`
- 상태 규약: `projectId: string` — `""` = 미분류

- [ ] **Step 1: 구현**

1. import 추가:

```ts
import Select from "@/components/shared/Select";
import { createProject } from "@/lib/projects/actions";
import { PROJECT_COLORS } from "@/lib/projects/constants";
import { notifyProjectsChanged, useProjects } from "@/lib/projects/useProjects";
```

2. 상태 추가(다른 useState 옆):

```ts
  const [projectId, setProjectId] = useState("");
  const [newProjectOpen, setNewProjectOpen] = useState(false);
  const [newProjectName, setNewProjectName] = useState("");
  const [newProjectColor, setNewProjectColor] = useState<string>(PROJECT_COLORS[0]);
  const [creatingProject, setCreatingProject] = useState(false);
  const { activeProjects } = useProjects();
```

3. 초안 저장 — `persistCurrentDraft`의 `hasContent`에 `|| projectId !== ""` 추가, `saveWorkTimelineDraft` 페이로드에 `projectId: projectId || null,` 추가, useCallback 의존성에 `projectId` 추가
4. 초안 복구 — `applyDraft`에 `setProjectId(draft.projectId ?? "");` 추가. `discardDraft`에 `setProjectId("");` 추가
5. 새 프로젝트 생성 핸들러 (컴포넌트 함수 내):

```ts
  async function handleCreateProject() {
    if (!newProjectName.trim() || creatingProject) return;
    setCreatingProject(true);
    try {
      const project = await createProject(newProjectName, newProjectColor);
      notifyProjectsChanged();
      setProjectId(project.id);
      setNewProjectOpen(false);
      setNewProjectName("");
      setNewProjectColor(PROJECT_COLORS[0]);
      setAutosaveArmed(true);
      toast.success(`'${project.name}' 프로젝트를 만들었습니다.`);
    } catch (error) {
      toast.error(error instanceof Error ? error.message : "프로젝트를 만들지 못했습니다.");
    } finally {
      setCreatingProject(false);
    }
  }
```

6. `handleSubmit`의 `createWorkTimelineEntry` 호출에 `projectId: projectId || null,` 추가
7. UI — "완료 시간" 필드 div 와 "파일 첨부" div 사이에 추가:

```tsx
          <div>
            <label className="mb-2 block text-sm font-bold text-slate-700">
              프로젝트 <span className="font-normal text-slate-400">(선택)</span>
            </label>
            <Select
              value={projectId}
              onChange={(v) => {
                setProjectId(v);
                setAutosaveArmed(true);
              }}
              ariaLabel="프로젝트 선택"
              className="w-full rounded-lg border border-slate-200 px-3.5 py-3 text-sm text-slate-700 sm:max-w-xs"
              options={[
                { value: "", label: "미분류" },
                ...activeProjects.map((project) => ({ value: project.id, label: project.name })),
              ]}
              footerAction={{ label: "새 프로젝트 만들기", onClick: () => setNewProjectOpen(true) }}
            />
            {newProjectOpen && (
              <div className="mt-2 space-y-2 rounded-lg border border-slate-200 bg-slate-50 p-3">
                <input
                  value={newProjectName}
                  onChange={(event) => setNewProjectName(event.target.value)}
                  maxLength={50}
                  placeholder="새 프로젝트 이름 (예: 코스피랩)"
                  className="w-full rounded-md border border-slate-200 bg-white px-3 py-2 text-sm text-slate-700 outline-none focus:border-indigo-400 focus:ring-2 focus:ring-indigo-100"
                />
                <div className="flex flex-wrap items-center gap-1.5" role="group" aria-label="프로젝트 색상">
                  {PROJECT_COLORS.map((color) => (
                    <button
                      key={color}
                      type="button"
                      onClick={() => setNewProjectColor(color)}
                      aria-label={`색상 ${color}`}
                      aria-pressed={newProjectColor === color}
                      className={`h-6 w-6 rounded-full border-2 transition-transform ${
                        newProjectColor === color ? "scale-110 border-slate-700" : "border-transparent"
                      }`}
                      style={{ backgroundColor: color }}
                    />
                  ))}
                </div>
                <div className="flex justify-end gap-2">
                  <button
                    type="button"
                    onClick={() => setNewProjectOpen(false)}
                    className="rounded-md px-3 py-1.5 text-xs font-bold text-slate-500 hover:bg-slate-200"
                  >
                    취소
                  </button>
                  <button
                    type="button"
                    onClick={() => void handleCreateProject()}
                    disabled={creatingProject || !newProjectName.trim()}
                    className="rounded-md bg-indigo-600 px-3 py-1.5 text-xs font-bold text-white hover:bg-indigo-500 disabled:bg-slate-300"
                  >
                    {creatingProject ? "만드는 중..." : "만들기"}
                  </button>
                </div>
              </div>
            )}
          </div>
```

- [ ] **Step 2: 검증 + 커밋**

Run: `npm run lint`; 수동 확인(선택): `npm run dev` 후 작성 모달에서 프로젝트 선택/생성.

```bash
git add jdi-portal/src/components/dashboard/work-timeline/WorkTimelineCreateModal.tsx
git commit -m "기능: 타임라인 작성 모달 프로젝트 선택 + 인라인 새 프로젝트"
```

---

### Task 7: 상세 화면 프로젝트 표시/변경

**Files:**
- Modify: `src/components/dashboard/work-timeline/WorkTimelineDetailClient.tsx`

**Interfaces:**
- Consumes: `UpdateWorkTimelineEntryInput.projectId`(Task 3), `useProjects`(Task 2)
- 규약: 보기 모드 = 색 점+이름 배지, 편집 모드(본인 글) = Select 로 변경. 저장 후 `entry.project` 를 projects 목록에서 찾아 로컬 반영.

- [ ] **Step 1: 구현**

1. import 추가: `import Select from "@/components/shared/Select";`, `import { useProjects } from "@/lib/projects/useProjects";`
2. 상태 추가: `const [projectId, setProjectId] = useState(initialEntry.project_id ?? "");` + `const { projects } = useProjects();`
3. `resetForm`에 `setProjectId(entry.project_id ?? "");` 추가
4. `handleSave`의 `updateWorkTimelineEntry` 호출에 `projectId: projectId || null,` 추가하고, 성공 시:

```ts
      const nextProject = projectId
        ? (projects.find((project) => project.id === projectId) ?? null)
        : null;
      setEntry((current) => ({
        ...current,
        ...updated,
        project: nextProject
          ? { id: nextProject.id, name: nextProject.name, color: nextProject.color }
          : null,
      }));
```

(기존 `setEntry((current) => ({ ...current, ...updated }));` 교체)
5. 편집 폼 — "완료 시간" label 블록 아래에 추가:

```tsx
              <label className="block max-w-xs">
                <span className="mb-1.5 block text-xs font-bold text-slate-500">프로젝트</span>
                <Select
                  value={projectId}
                  onChange={(v) => setProjectId(v)}
                  ariaLabel="프로젝트 선택"
                  className="h-10 w-full rounded-md border border-slate-200 bg-white px-3 text-sm text-slate-700"
                  options={[
                    { value: "", label: "미분류" },
                    ...projects
                      .filter((project) => !project.is_archived || project.id === projectId)
                      .map((project) => ({ value: project.id, label: project.name })),
                  ]}
                />
              </label>
```

6. 보기 모드 메타 줄 — `formatCompletedAt` span 다음(연결된 할일 Link 앞)에 추가:

```tsx
                {entry.project && (
                  <span className="inline-flex items-center gap-1.5 rounded-md bg-slate-100 px-2 py-0.5 text-xs font-bold text-slate-600">
                    <span
                      className="h-2 w-2 rounded-full"
                      style={{ backgroundColor: entry.project.color }}
                      aria-hidden="true"
                    />
                    {entry.project.name}
                  </span>
                )}
```

- [ ] **Step 2: 검증 + 커밋**

Run: `npm run lint`

```bash
git add jdi-portal/src/components/dashboard/work-timeline/WorkTimelineDetailClient.tsx
git commit -m "기능: 타임라인 상세 프로젝트 배지 표시·본인 글 프로젝트 변경"
```

---

### Task 8: 프로젝트 관리 모달

**Files:**
- Create: `src/components/dashboard/work-timeline/ProjectManageModal.tsx`
- Modify: `src/components/dashboard/work-timeline/WorkTimelineSection.tsx` (모달 렌더 연결)

**Interfaces:**
- Consumes: `useProjects`/`notifyProjectsChanged`, `updateProject`/`deleteProject`/`createProject`, `PROJECT_COLORS`, `ModalContainer`
- Produces: `<ProjectManageModal currentUserRole={string} onClose={() => void} />`

- [ ] **Step 1: ProjectManageModal.tsx 작성**

```tsx
"use client";

import { useState } from "react";
import { X } from "phosphor-react";
import { toast } from "sonner";
import ModalContainer from "@/components/shared/ModalContainer";
import { createProject, deleteProject, updateProject } from "@/lib/projects/actions";
import { PROJECT_COLORS, PROJECT_NAME_MAX_LENGTH } from "@/lib/projects/constants";
import { notifyProjectsChanged, useProjects } from "@/lib/projects/useProjects";
import type { Project } from "@/lib/projects/types";

interface ProjectManageModalProps {
  currentUserRole: string;
  onClose: () => void;
}

function ProjectRow({ project, isAdmin }: { project: Project; isAdmin: boolean }) {
  const [name, setName] = useState(project.name);
  const [busy, setBusy] = useState(false);
  const [confirmDelete, setConfirmDelete] = useState(false);

  async function run(operation: () => Promise<unknown>, successMessage: string) {
    if (busy) return;
    setBusy(true);
    try {
      await operation();
      notifyProjectsChanged();
      toast.success(successMessage);
    } catch (error) {
      toast.error(error instanceof Error ? error.message : "처리하지 못했습니다.");
    } finally {
      setBusy(false);
    }
  }

  return (
    <li className={`rounded-lg border border-slate-100 p-3 ${project.is_archived ? "bg-slate-50" : "bg-white"}`}>
      <div className="flex items-center gap-2">
        <span
          className="h-3 w-3 shrink-0 rounded-full"
          style={{ backgroundColor: project.color }}
          aria-hidden="true"
        />
        <input
          value={name}
          onChange={(event) => setName(event.target.value)}
          maxLength={PROJECT_NAME_MAX_LENGTH}
          disabled={busy}
          aria-label={`${project.name} 이름 수정`}
          className="min-w-0 flex-1 rounded-md border border-transparent bg-transparent px-2 py-1 text-sm font-bold text-slate-700 outline-none focus:border-indigo-300 focus:bg-white"
        />
        {name.trim() !== project.name && (
          <button
            type="button"
            disabled={busy || !name.trim()}
            onClick={() => void run(() => updateProject(project.id, { name }), "이름을 변경했습니다.")}
            className="shrink-0 rounded-md bg-indigo-600 px-2.5 py-1 text-xs font-bold text-white hover:bg-indigo-500 disabled:bg-slate-300"
          >
            저장
          </button>
        )}
      </div>
      <div className="mt-2 flex flex-wrap items-center justify-between gap-2">
        <div className="flex flex-wrap gap-1.5" role="group" aria-label={`${project.name} 색상`}>
          {PROJECT_COLORS.map((color) => (
            <button
              key={color}
              type="button"
              disabled={busy}
              onClick={() => void run(() => updateProject(project.id, { color }), "색상을 변경했습니다.")}
              aria-label={`색상 ${color}`}
              aria-pressed={project.color === color}
              className={`h-5 w-5 rounded-full border-2 transition-transform ${
                project.color === color ? "scale-110 border-slate-700" : "border-transparent"
              }`}
              style={{ backgroundColor: color }}
            />
          ))}
        </div>
        <div className="flex items-center gap-1.5">
          <button
            type="button"
            disabled={busy}
            onClick={() => void run(
              () => updateProject(project.id, { isArchived: !project.is_archived }),
              project.is_archived ? "보관을 해제했습니다." : "보관 처리했습니다. 사이드바에서 숨겨집니다.",
            )}
            className="rounded-md border border-slate-200 bg-white px-2.5 py-1 text-xs font-bold text-slate-500 hover:bg-slate-50"
          >
            {project.is_archived ? "보관 해제" : "보관"}
          </button>
          {isAdmin && !confirmDelete && (
            <button
              type="button"
              disabled={busy}
              onClick={() => setConfirmDelete(true)}
              className="rounded-md border border-red-200 bg-white px-2.5 py-1 text-xs font-bold text-red-600 hover:bg-red-50"
            >
              삭제
            </button>
          )}
        </div>
      </div>
      {confirmDelete && (
        <div className="mt-2 rounded-md border border-red-200 bg-red-50 px-3 py-2">
          <p className="text-xs font-bold text-red-700">
            삭제해도 글은 남고 &lsquo;미분류&rsquo;로 바뀝니다. 삭제할까요?
          </p>
          <div className="mt-1.5 flex justify-end gap-1.5">
            <button
              type="button"
              onClick={() => setConfirmDelete(false)}
              className="rounded-md px-2.5 py-1 text-xs font-bold text-slate-500 hover:bg-white"
            >
              취소
            </button>
            <button
              type="button"
              disabled={busy}
              onClick={() => void run(() => deleteProject(project.id), "프로젝트를 삭제했습니다.")}
              className="rounded-md bg-red-600 px-2.5 py-1 text-xs font-bold text-white hover:bg-red-500 disabled:bg-slate-300"
            >
              삭제
            </button>
          </div>
        </div>
      )}
    </li>
  );
}

export default function ProjectManageModal({ currentUserRole, onClose }: ProjectManageModalProps) {
  const { projects, loaded } = useProjects();
  const [newName, setNewName] = useState("");
  const [newColor, setNewColor] = useState<string>(PROJECT_COLORS[0]);
  const [creating, setCreating] = useState(false);
  const isAdmin = currentUserRole === "admin";
  const activeList = projects.filter((project) => !project.is_archived);
  const archivedList = projects.filter((project) => project.is_archived);

  async function handleCreate() {
    if (!newName.trim() || creating) return;
    setCreating(true);
    try {
      const project = await createProject(newName, newColor);
      notifyProjectsChanged();
      setNewName("");
      setNewColor(PROJECT_COLORS[0]);
      toast.success(`'${project.name}' 프로젝트를 만들었습니다.`);
    } catch (error) {
      toast.error(error instanceof Error ? error.message : "프로젝트를 만들지 못했습니다.");
    } finally {
      setCreating(false);
    }
  }

  return (
    <ModalContainer onClose={onClose} maxWidth="max-w-lg" className="max-h-[85vh] overflow-hidden !rounded-lg !p-0">
      <div className="flex items-center justify-between border-b border-slate-100 px-5 py-4">
        <div>
          <h2 className="text-lg font-bold text-slate-800">프로젝트 관리</h2>
          <p className="mt-1 text-xs text-slate-400">이름·색상 수정, 보관, {isAdmin ? "삭제(관리자)" : "삭제는 관리자만"} 가능합니다.</p>
        </div>
        <button
          type="button"
          onClick={onClose}
          aria-label="프로젝트 관리 닫기"
          className="flex h-9 w-9 items-center justify-center rounded-lg text-slate-400 hover:bg-slate-100 hover:text-slate-600"
        >
          <X size={18} aria-hidden="true" />
        </button>
      </div>

      <div className="max-h-[calc(85vh-72px)] space-y-4 overflow-y-auto px-5 py-4">
        <div className="rounded-lg border border-slate-200 bg-slate-50 p-3">
          <p className="text-xs font-bold text-slate-600">새 프로젝트</p>
          <div className="mt-2 flex gap-2">
            <input
              value={newName}
              onChange={(event) => setNewName(event.target.value)}
              maxLength={PROJECT_NAME_MAX_LENGTH}
              placeholder="프로젝트 이름"
              className="min-w-0 flex-1 rounded-md border border-slate-200 bg-white px-3 py-2 text-sm text-slate-700 outline-none focus:border-indigo-400 focus:ring-2 focus:ring-indigo-100"
            />
            <button
              type="button"
              onClick={() => void handleCreate()}
              disabled={creating || !newName.trim()}
              className="shrink-0 rounded-md bg-indigo-600 px-3 py-2 text-xs font-bold text-white hover:bg-indigo-500 disabled:bg-slate-300"
            >
              {creating ? "만드는 중..." : "만들기"}
            </button>
          </div>
          <div className="mt-2 flex flex-wrap gap-1.5" role="group" aria-label="새 프로젝트 색상">
            {PROJECT_COLORS.map((color) => (
              <button
                key={color}
                type="button"
                onClick={() => setNewColor(color)}
                aria-label={`색상 ${color}`}
                aria-pressed={newColor === color}
                className={`h-5 w-5 rounded-full border-2 transition-transform ${
                  newColor === color ? "scale-110 border-slate-700" : "border-transparent"
                }`}
                style={{ backgroundColor: color }}
              />
            ))}
          </div>
        </div>

        {!loaded ? (
          <p className="py-6 text-center text-sm font-semibold text-slate-400">불러오는 중...</p>
        ) : activeList.length === 0 && archivedList.length === 0 ? (
          <p className="py-6 text-center text-sm font-semibold text-slate-400">아직 프로젝트가 없습니다.</p>
        ) : (
          <>
            <ul className="space-y-2" aria-label="프로젝트 목록">
              {activeList.map((project) => (
                <ProjectRow key={project.id} project={project} isAdmin={isAdmin} />
              ))}
            </ul>
            {archivedList.length > 0 && (
              <div>
                <p className="mb-2 text-xs font-bold text-slate-400">보관된 프로젝트</p>
                <ul className="space-y-2" aria-label="보관된 프로젝트 목록">
                  {archivedList.map((project) => (
                    <ProjectRow key={project.id} project={project} isAdmin={isAdmin} />
                  ))}
                </ul>
              </div>
            )}
          </>
        )}
      </div>
    </ModalContainer>
  );
}
```

- [ ] **Step 2: Section 연결 (버튼·상태·렌더)**

`WorkTimelineSection.tsx`:
- import 추가: `import ProjectManageModal from "./ProjectManageModal";`
- 상태 추가(다른 useState 옆): `const [manageOpen, setManageOpen] = useState(false);`
- 헤더의 "업무 추가" 버튼 앞에 (compact 아닐 때만):

```tsx
            {!compact && (
              <button
                type="button"
                onClick={() => setManageOpen(true)}
                className="inline-flex items-center gap-1 rounded-lg border border-slate-200 bg-white px-2.5 py-1.5 text-[11px] font-bold text-slate-600 transition-colors hover:bg-slate-50 sm:gap-1.5 sm:px-3 sm:py-2 sm:text-xs"
              >
                프로젝트 관리
              </button>
            )}
```

- 파일 끝 `{createOpen && (...)}` 옆에 추가:

```tsx
      {manageOpen && (
        <ProjectManageModal
          currentUserRole={currentUserRole}
          onClose={() => setManageOpen(false)}
        />
      )}
```

- [ ] **Step 3: 검증 + 커밋**

Run: `npm run lint`

```bash
git add jdi-portal/src/components/dashboard/work-timeline/ProjectManageModal.tsx jdi-portal/src/components/dashboard/work-timeline/WorkTimelineSection.tsx
git commit -m "기능: 프로젝트 관리 모달(이름·색상·보관·admin 삭제)"
```

---

### Task 9: 할일(tasks) 연동

**Files:**
- Modify: `src/lib/tasks/types.ts`
- Modify: `src/lib/tasks/queries.ts` (`TASK_BASE_SELECT`, `TaskHistoryFilters`, `getTaskHistoryWithDetails`)
- Modify: `src/lib/tasks/fast-queries.ts` (pg 쿼리 jsonb)
- Modify: `src/lib/tasks/actions.ts` (`createTask`/`updateTask`)
- Modify: `src/components/dashboard/tasks/TaskCreateModal.tsx`
- Modify: `src/components/dashboard/tasks/detail/TaskDetailClient.tsx` (패널·페이지 두 모드)
- Modify: `src/components/dashboard/tasks/TasksPageClient.tsx` (기록 프로젝트 필터 + 카드 배지)
- Modify: `docs/superpowers/specs/2026-07-22-projects-design.md` (그룹 보기 → 기록 필터로 스펙 갱신)
- Modify: `scripts/projects-feature.test.mjs`

**Interfaces:**
- Consumes: `ProjectRef`, `useProjects`
- Produces:
  - `Task.project_id: string | null`, `TaskWithDetails.project?: ProjectRef | null` (RPC 경로엔 없을 수 있어 optional)
  - `TaskHistoryFilters.projectId?: string` (`"none"` | UUID)
  - `createTask`/`updateTask` params 에 `projectId?: string | null`

- [ ] **Step 1: 테스트 블록 추가**

```js
test("할일: project 를 빠른 경로와 REST 폴백 양쪽에 싣는다 (성능 불변조건 3)", () => {
  const fast = read("src/lib/tasks/fast-queries.ts");
  assert.match(fast, /'project_id', t\.project_id/);
  assert.match(fast, /'project',[\s\S]*?public\.projects pj/);
  const queries = read("src/lib/tasks/queries.ts");
  assert.match(queries, /project:projects\(id, name, color\)/);
  assert.match(queries, /is\("project_id", null\)/);
  const actions = read("src/lib/tasks/actions.ts");
  assert.match(actions, /project_id/);
});
```

Run → FAIL 확인

- [ ] **Step 2: types.ts**

- import 추가: `import type { ProjectRef } from "@/lib/projects/types";`
- `Task`의 `category: string | null;` 아래 추가: `project_id: string | null;`
- `TaskWithDetails`에 추가: `project?: ProjectRef | null;`

- [ ] **Step 3: queries.ts**

TASK_BASE_SELECT 교체:

```ts
const TASK_BASE_SELECT = `
  id, title, description, status, priority, category, project_id,
  due_date, start_date, position, parent_id, created_by,
  created_at, updated_at, completed_at,
  creator_profile:profiles!tasks_created_by_fkey(full_name, avatar_url),
  project:projects(id, name, color)
`;
```

`TaskHistoryFilters`에 추가: `projectId?: string; // "none" = 미분류`
`getTaskHistoryWithDetails`의 `if (filters.status) ...` 다음에 추가:

```ts
  if (filters.projectId === "none") query = query.is("project_id", null);
  else if (filters.projectId) query = query.eq("project_id", filters.projectId);
```

- [ ] **Step 4: fast-queries.ts**

`INITIAL_TASKS_PAGE_QUERY`의 jsonb에서 `'category', t.category,` 다음에 추가:

```
          'project_id', t.project_id,
          'project', (
            select jsonb_build_object('id', pj.id, 'name', pj.name, 'color', pj.color)
            from public.projects pj
            where pj.id = t.project_id
          ),
```

(REST 폴백은 Step 3 의 TASK_BASE_SELECT 로 이미 반영 — 양쪽 동시 수정 완료)

- [ ] **Step 5: actions.ts**

- `createTask` params 에 `projectId?: string | null;` 추가, insert 페이로드 `category` 다음에 `project_id: params.projectId || null,` 추가
- `updateTask` params 에 `projectId?: string | null;` 추가. update 페이로드를 만드는 부분에서 `params.projectId !== undefined` 일 때만 `project_id: params.projectId` 포함 (기존 category 처리 방식과 동일한 위치 — updateTask 내부의 `.update({...})` 객체에 조건부로 추가:

```ts
      ...(params.projectId !== undefined ? { project_id: params.projectId } : {}),
```

updateTask 의 update 객체가 어떻게 구성되어 있는지 해당 파일에서 확인 후 같은 스타일로 넣는다 — status 처리 아래 일반 필드 업데이트 블록에 위치)

- [ ] **Step 6: TaskCreateModal.tsx**

- import 추가: `import { useProjects } from "@/lib/projects/useProjects";`
- 상태 추가: `const [projectId, setProjectId] = useState("");` + `const { activeProjects } = useProjects();`
- `createTask` 호출에 `projectId: projectId || null,` 추가
- UI — "데드라인" div 다음에 추가:

```tsx
        <div>
          <label className="mb-1 block text-sm font-semibold text-slate-700">프로젝트</label>
          <Select
            value={projectId}
            onChange={(v) => setProjectId(v)}
            ariaLabel="프로젝트 선택"
            className="glass-input w-full rounded-xl px-4 py-2.5 text-sm outline-none"
            options={[
              { value: "", label: "미분류" },
              ...activeProjects.map((project) => ({ value: project.id, label: project.name })),
            ]}
          />
        </div>
```

- [ ] **Step 7: TaskDetailClient.tsx (패널·페이지 두 모드 모두)**

- import 추가: `import { useProjects } from "@/lib/projects/useProjects";`
- 상태(`category` useState 옆): `const [projectId, setProjectId] = useState(task.project_id ?? "");` + `const { projects } = useProjects();`
- 재동기화 effect 에 `setProjectId(task.project_id ?? "");` 추가 + 의존성 `task.project_id`
- `handleSave`의 `updateTask` 호출에 `projectId: projectId || null,` 추가
- 옵션 목록(컴포넌트 내 공용):

```ts
  const projectOptions = [
    { value: "", label: "미분류" },
    ...projects
      .filter((project) => !project.is_archived || project.id === projectId)
      .map((project) => ({ value: project.id, label: project.name })),
  ];
```

- **패널 모드**: "업무 분류" 블록(`sm:col-span-2`) 앞에 추가:

```tsx
            <div className="sm:col-span-2">
              <label className="mb-1 block text-xs font-bold text-slate-400">프로젝트</label>
              <Select
                ariaLabel="프로젝트"
                value={projectId}
                onChange={(v) => setProjectId(v)}
                disabled={!canEdit}
                className="glass-input w-full rounded-lg px-3 py-2 text-sm outline-none disabled:bg-slate-50"
                options={projectOptions}
              />
            </div>
```

- **페이지 모드**(파일 후반부, 같은 구조의 "업무 분류" 필드가 한 번 더 있음): 동일한 블록을 같은 위치에 추가

- [ ] **Step 8: TasksPageClient.tsx**

1. import 추가: `import { useProjects } from "@/lib/projects/useProjects";`
2. 상태: `const [historyProject, setHistoryProject] = useState("");` + `const { activeProjects } = useProjects();`
3. `historyFilters` useMemo 에 `projectId: historyProject || undefined,` 추가 + 의존성 `historyProject`
4. 기록 필터 그리드(`sm:grid-cols-2 lg:grid-cols-[minmax(0,1fr)_180px]`)를 `sm:grid-cols-2 lg:grid-cols-[minmax(0,1fr)_170px_170px]` 로 바꾸고 상태 Select 다음에 추가:

```tsx
            <Select
              value={historyProject}
              onChange={(v) => setHistoryProject(v)}
              ariaLabel="프로젝트 필터"
              className="h-9 w-full rounded-md border border-slate-200 bg-white px-3 text-xs font-semibold text-slate-600 outline-none focus:border-indigo-400 focus:ring-2 focus:ring-indigo-100"
              options={[
                { value: "", label: "전체 프로젝트" },
                ...activeProjects.map((project) => ({ value: project.id, label: project.name })),
                { value: "none", label: "미분류" },
              ]}
            />
```

5. 카드 배지 — "오늘 할 일" 목록의 `<p className="truncate text-sm font-bold text-slate-800">{task.title}</p>` 를:

```tsx
                      <div className="flex min-w-0 items-center gap-1.5">
                        {task.project && (
                          <span className="inline-flex max-w-24 shrink-0 items-center gap-1 rounded-md bg-slate-100 px-1.5 py-0.5 text-[10px] font-bold text-slate-600">
                            <span className="h-1.5 w-1.5 shrink-0 rounded-full" style={{ backgroundColor: task.project.color }} aria-hidden="true" />
                            <span className="truncate">{task.project.name}</span>
                          </span>
                        )}
                        <p className="min-w-0 truncate text-sm font-bold text-slate-800">{task.title}</p>
                      </div>
```

"할 일 기록" 목록의 동일한 title `<p>` 도 같은 방식으로 교체

- [ ] **Step 9: 스펙 문서 갱신**

`docs/superpowers/specs/2026-07-22-projects-design.md`의 "## 5) 할일(tasks) 화면" 중 "그룹 보기 옵션에 "프로젝트별" 추가" 항목을 다음으로 교체:

```
- (변경) 그룹 보기 UI는 현재 화면에서 사용되지 않는 구버전 잔재로 확인되어,
  대신 **할 일 기록에 프로젝트 필터**(전체/프로젝트/미분류)를 추가한다.
```

- [ ] **Step 10: 검증 + 커밋**

Run: `node --test scripts/projects-feature.test.mjs` → PASS, `npm run lint`

```bash
git add jdi-portal/src/lib/tasks jdi-portal/src/components/dashboard/tasks jdi-portal/scripts/projects-feature.test.mjs jdi-portal/docs/superpowers/specs/2026-07-22-projects-design.md
git commit -m "기능: 할일 프로젝트 연동(빠른경로+REST 양쪽, 작성·상세·기록 필터·배지)"
```

---

### Task 10: 전체 검증 (성능 회귀 포함)

**Files:** 없음 (검증만)

- [ ] **Step 1: 정적 테스트 전체**

Run: `cd jdi-portal; node --test scripts/projects-feature.test.mjs`
Expected: 전부 PASS

- [ ] **Step 2: 성능 회귀 테스트 (필수)**

Run: `npm run test:performance`
Expected: 40개 검사 전부 PASS. 실패 시 해당 불변조건을 깬 변경을 되돌리거나 수정 (특히 fast-queries 수정분).

- [ ] **Step 3: 린트/빌드**

Run: `npm run lint` → 오류 없음
Run: `npm run build` → 성공. `useSearchParams` Suspense 오류가 나면 Task 4 참고 사항대로 하위 메뉴를 Suspense 로 감싼다.

- [ ] **Step 4: 수정이 생겼으면 커밋**

```bash
git add -A
git commit -m "정리: 프로젝트 기능 검증 반영"
```

(수정이 없으면 커밋 생략)

---

### Task 11: 운영 DB 적용 (⚠️ 사용자 확인 게이트)

**Files:** 없음

- [ ] **Step 1: 사용자에게 최종 확인**

다음을 알기 쉽게 보고하고 진행 여부를 묻는다 (AskUserQuestion):
- 적용 내용: projects 테이블 생성, 타임라인·할일에 project_id 추가, 초기 프로젝트 3개(코스피랩·TMA·JDI 포탈), 기존 글 제목 접두어 자동 분류 **+ 접두어 제거(되돌릴 수 없음)**, "트리/오너먼트" 포함 타임라인 글 → TMA
- 주의: '트리'라는 단어가 들어간 무관한 글도 TMA 로 분류될 수 있음 (수정 화면에서 개별 변경 가능)

- [ ] **Step 2: 승인 시에만 적용**

Run: `cd jdi-portal; echo y | npx supabase db push --linked`
Expected: `101_projects.sql` 적용 성공. (과거 마이그레이션 드리프트 이력이 있으므로 오류 시 `npx supabase migration list --linked` 로 상태 확인 후 보고)

- [ ] **Step 3: 적용 후 확인**

- 사이트에서: 사이드바 하위 메뉴 표시, 기존 "코스피랩 - ..." 글이 배지+정리된 제목으로 보이는지, 새 글 작성 시 프로젝트 선택 동작 확인
- 문제 발견 시 즉시 보고 (자동 분류 결과는 글 수정 화면에서 개별 정정 가능)
