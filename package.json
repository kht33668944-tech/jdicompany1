{
  "name": "jdicompany-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@11.9.0",
  "description": "Root wrapper so Railpack detects Node and builds jdi-portal subdirectory",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "cd jdi-portal && npm run dev",
    "build": "cd jdi-portal && npm ci && npm run build",
    "start": "cd jdi-portal && npm start"
  }
}
